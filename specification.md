# トリッカル装備設計図・最小周回計算ツール 仕様書

## 1. 文書情報

- 文書種別: MVP仕様書
- 作成日: 2026-08-04
- 対象: 個人利用向けスマートフォンWebアプリ
- 参照情報: [トリッカル・もちもちほっペ大作戦 Wiki「装備設計図」](https://wikiwiki.jp/thetrickal/%E8%A3%85%E5%82%99%E8%A8%AD%E8%A8%88%E5%9B%B3)

## 2. 目的

利用者が必要としている複数の装備設計図と必要数を入力すると、必要数をすべて満たす周回方法のうち、総周回数が最小になるステージの組み合わせを計算して表示する。

実際のドロップ率には依存せず、すべての登録済みドロップを1周ごとに100%入手できるものとして扱う。

## 3. MVPの基本方針

- スマートフォンのブラウザから利用できるWebアプリとする。
- 専用APIサーバー、ユーザーアカウント、クラウドデータベースは使用しない。
- GitHub Pagesへ配置できる静的PWA構成とする。
- 初回表示後は、通信できない状態でも主要機能を利用できるようにする。
- 初期マスターデータには、参照Wikiに掲載されている現在の設計図とステージのドロップテーブルを収録する。
- マスターデータはアプリ上で追加、編集、削除できるようにする。
- 編集したマスターデータと画像は、使用中のブラウザ内に保存する。
- 通常の計算入力と計算結果は保存しない。
- データのバックアップと復元にはJSONファイルを使用する。

## 4. 用語

| 用語 | 定義 |
|---|---|
| 設計図 | 計算対象となる装備素材。Wiki上の「設計図」「欠片」をまとめて本アプリでは設計図として扱う。 |
| ステージ | 周回可能な進軍ステージ。例: `30-10`。 |
| ドロップ | 1周したときに獲得する設計図。Wiki上の「設計図」と「副産物」の両方を含む。 |
| 必要数 | 利用者が獲得したい設計図の個数。 |
| 獲得数 | 計算された周回方法によって獲得できる設計図の個数。 |
| 余剰数 | `獲得数 - 必要数`。必要数を超えて獲得する個数。 |
| 総周回数 | 結果に含まれる全ステージの周回回数の合計。 |
| マスターデータ | 設計図、アイコン画像、ステージ、ドロップテーブルのデータ。 |

## 5. 対象範囲

### 5.1 MVPに含める機能

1. 必要な設計図の選択
2. 設計図ごとの必要数の数値入力
3. 複数の設計図の同時指定
4. 総周回数が最小になる周回方法の厳密探索
5. ステージごとの周回数表示
6. 設計図ごとの必要数、獲得数、余剰数表示
7. 初期マスターデータの収録
8. 設計図の追加、編集、削除
9. ステージとドロップテーブルの追加、編集、削除
10. アイコン画像の登録と表示
11. マスターデータのJSON書き出しと読み込み
12. 初期マスターデータへのリセット
13. スマートフォン向けレイアウト
14. PWAとしてのオフライン利用

### 5.2 MVPに含めない機能

- 実際のドロップ率を使用した期待値計算
- 「装備の定石」の効率計算
- キャンディ消費量の最適化
- 未解放ステージの除外
- 所持数、計算履歴、前回入力の保存
- ユーザー登録、ログイン、端末間同期
- 複数利用者でのデータ共有
- Wikiからの自動取得、自動更新
- 同率となるすべての最適解の表示
- 一般公開を前提とした運用機能

## 6. 計算仕様

### 6.1 入力

計算対象として、1件以上の次の組を受け取る。

- 設計図ID
- 必要数

同じ設計図を入力一覧へ重複登録することはできない。

### 6.2 入力値の制約

- 必要数は1以上の整数とする。
- 必要数は数値入力欄へ直接入力する。
- `+`、`-`ボタンは設けない。
- 空欄、0、負数、小数、数値以外はエラーとする。
- MVPでの必要数の上限は999とする。
- 少なくとも1件の有効な入力がなければ計算を開始できない。

### 6.3 ドロップの扱い

- 各ステージを1周すると、そのステージに登録されたすべてのドロップを登録個数どおり獲得する。
- 初期データでは、Wiki表の「設計図」と「副産物」をそれぞれ1個獲得する。
- ドロップ率は常に100%として扱う。
- 同じ設計図が同一ステージの複数枠に登録されている場合、個数を合算する。
- 計算対象として指定されていない設計図の獲得は、総周回数と余剰数の評価に影響させない。

### 6.4 最適化条件

ステージ `s` の周回数を0以上の整数 `x_s`、設計図 `i` の必要数を `d_i`、ステージ `s` で得られる設計図 `i` の個数を `a_i,s` とする。

すべての指定設計図について次を満たすこと。

```text
Σ(a_i,s × x_s) >= d_i
```

第一目的は次の値の最小化とする。

```text
総周回数 = Σx_s
```

### 6.5 同率解の決定規則

総周回数が同じ解が複数存在する場合、代表となる1件だけを表示する。毎回同じ入力から同じ結果を得られるよう、次の順で決定する。

1. 指定された設計図の余剰数合計が少ない解
2. 使用するステージの種類数が少ない解
3. ステージ番号を文字列の昇順で比較したときに先となる解

この規則は同率解を一意に選ぶためのもので、総周回数の最小化より優先してはならない。

### 6.6 計算不能

次の場合は計算不能として、不足している設計図名を表示する。

- 指定された設計図をドロップする有効なステージが存在しない。
- マスターデータの不整合により計算できない。

計算不能な設計図が1件でもある場合、部分的な周回結果は表示しない。

### 6.7 実装要件

- 近似解ではなく、総周回数が最小であることを保証できる整数最適化を使用する。
- 計算処理はWeb Worker等を使用し、計算中も画面操作を阻害しない構成とする。
- 計算中は進行中表示を行う。入力が変わった場合は実行中の計算を破棄し、新しい条件で再計算する。
- 一般的な入力では、スマートフォン上で3秒以内の結果表示を目標とする。
- 上限時間を超えた場合、近似結果を最適解として表示してはならない。

## 7. 画面仕様

### 7.1 共通ナビゲーション

主要画面は次の3つとする。

- 周回計算
- データ編集
- バックアップ

片手操作を考慮し、主要な切り替え操作は画面下部に配置する。

### 7.2 周回計算画面

#### 設計図の追加

- 「設計図を追加」をタップすると設計図選択画面を開く。
- 設計図選択画面では、アイコンと名称を一覧表示する。
- ランクと装備分類は、それぞれ一度に複数条件を選んで絞り込めるようにする。
- 名称検索は設けない。
- 設計図は一度に複数選択でき、「確定」をタップすると入力一覧へまとめて追加する。
- すでに入力一覧へ追加済みの設計図は再選択できない状態で表示する。
- 設計図の選択はタップだけで完結する。

#### 必要数の入力

- 選択した各設計図について数値入力欄を表示する。
- 設計図を追加した直後の必要数は空欄とし、利用者が直接入力する。
- スマートフォンでは数字キーボードが開く入力属性を使用する。
- 入力行には、アイコン、名称、必要数、削除操作を表示する。
- 必要数の変更に増減ボタンは使用しない。
- 必要数を連続して入力している間は入力欄を再生成せず、タップした順番どおりに数字を保持する。
- HTMLの数値欄に依存せず、数字キーボードを開く属性と数字のみを受け付ける検証を組み合わせる。

#### 計算実行

- 「最小周回を計算」ボタンは設けない。
- 設計図の追加、削除または必要数の変更後に、自動で最小周回結果を再計算する。
- 入力エラーがある場合は該当欄の近くに理由を表示し、有効な入力になるまで計算しない。
- 連続して必要数が変更された場合は直前の計算を破棄し、最後の入力を対象に計算する。
- 初期データに含まれる同一ランクの設計図6種類へ必要数42を指定した場合も、計算が長時間停止しないこと。
- 選択中の設計図に対するドロップ内容が同じステージが複数ある場合は、章番号、次にステージ番号が大きい後発ステージだけを計算候補に使用する。

### 7.3 計算結果画面

上から次の順で表示する。

1. 総周回数
2. ステージ別の周回方法
3. 指定設計図ごとの獲得内訳
4. 入力を修正して再計算する操作

#### ステージ別の表示内容

- ステージ名
- 周回数
- そのステージで獲得する全設計図のアイコンと名称
- 各ドロップの合計獲得数

ステージ一覧は、周回数の多い順、同数の場合はステージ番号の昇順で表示する。

#### 設計図別の表示内容

- アイコン
- 名称
- 必要数
- 獲得数
- 余剰数

余剰数が0の場合も0と表示する。

### 7.4 データ編集トップ画面

次の管理項目を表示する。

- 設計図管理
- ステージ管理
- 初期データへ戻す

通常利用時の誤操作を防ぐため、データ編集画面は周回計算画面から1回の誤タップで編集操作が実行されない構成とする。

### 7.5 設計図管理画面

#### 一覧

- アイコン
- 名称
- ランク
- 装備分類
- 編集操作
- 削除操作

ランクと装備分類は一覧の整理と選択画面の絞り込みに使用する。将来追加する設計図では未設定も許可する。

一覧と選択画面は次の順で表示する。

1. ランクまたは装備分類が未設定の設計図
2. ランクの高い順（ランク11はランク10より前）
3. 同じランクでは `鎧 → ネックレス → 帽子 → リング → ブーツ → 剣 → 杖`
4. 上記以外の分類は定義済み分類の後に五十音順

#### 追加・編集項目

- アイコン画像: 任意
- ランク: 任意の正の整数
- 装備分類: 任意の文字列

名称はランクと装備分類から自動生成し、入力項目を設けない。ランクと装備分類の組み合わせが既存の設計図と同じ場合は重複エラーとする。

#### アイコン画像

- PNG、JPEG、WebPを受け付ける。
- 元画像は切り抜き、変形を行わない。
- 表示枠内にアスペクト比を維持して縮小表示する。
- 画像が未登録の場合は共通の代替アイコンを表示する。
- 画像はブラウザ内データベースへ保存する。

#### 削除

- ステージから参照されている設計図は、そのまま削除できない。
- 参照しているステージを表示し、先にドロップ設定を変更するよう案内する。
- 未参照の設計図を削除するときは確認を表示する。

### 7.6 ステージ管理画面

#### 一覧

- ステージ名
- 登録ドロップ数
- 編集操作
- 削除操作

ステージは章番号の高い順、同じ章ではステージ番号の高い順で表示する。

#### 追加・編集項目

- 章番号: 1以上の整数、必須
- ステージ番号: 1以上の整数、必須
- ドロップする設計図: 1件以上必須
- 1周あたりの個数: 1以上の整数、初期値1

ステージ名は章番号とステージ番号から `章番号-ステージ番号` の形式で自動生成する。同じ章番号とステージ番号の組み合わせは重複エラーとする。

ドロップ欄は固定2枠にせず、将来の変更に備えて複数件を追加できる構造とする。

同じ設計図を複数行へ登録した場合、保存前に1行へ合算するか、重複エラーとして修正を促す。

#### 削除

- 削除前にステージ名を含む確認を表示する。
- 削除後、そのステージは新しい計算に使用しない。

### 7.7 初期データへのリセット

- 実行すると編集済みマスターデータと登録画像が失われることを明示する。
- 確認操作後にのみ実行する。
- 通常の計算入力には影響しない。
- リセット後はアプリ同梱の初期マスターデータを読み込む。

### 7.8 バックアップ画面

#### 書き出し

- 現在の全マスターデータを1つのJSONファイルとして保存する。
- 設計図、ステージ、ドロップ設定、登録画像、データ形式のバージョンを含める。
- ファイル名には書き出し日を含める。

ファイル名例:

```text
trickcal-blueprint-backup-2026-08-04.json
```

#### 読み込み

- JSONファイルを選択し、形式とデータ内容を検証する。
- エラーがある場合は現在のデータを変更せず、理由を表示する。
- 正常な場合は、現在の全マスターデータを置き換えることを確認してから復元する。
- 復元は一括処理とし、途中状態を保存しない。

## 8. データ仕様

### 8.1 設計図

```json
{
  "id": "item_uuid",
  "name": "ランク8 鎧",
  "rank": 8,
  "category": "鎧",
  "defaultIconPath": "./blueprint-icons/81.webp",
  "icon": {
    "mimeType": "image/webp",
    "data": "base64 encoded data"
  },
  "createdAt": "2026-08-04T00:00:00.000Z",
  "updatedAt": "2026-08-04T00:00:00.000Z"
}
```

### 8.2 ステージ

```json
{
  "id": "stage_uuid",
  "name": "30-10",
  "chapter": 30,
  "number": 10,
  "drops": [
    { "itemId": "item_83", "quantity": 1 },
    { "itemId": "item_94", "quantity": 1 }
  ],
  "createdAt": "2026-08-04T00:00:00.000Z",
  "updatedAt": "2026-08-04T00:00:00.000Z"
}
```

### 8.3 バックアップ形式

```json
{
  "schemaVersion": 1,
  "exportedAt": "2026-08-04T00:00:00.000Z",
  "items": [],
  "stages": []
}
```

- IDは名称変更の影響を受けない一意の値とする。
- ステージのドロップは存在する設計図IDだけを参照できる。
- バックアップ読み込み時に参照整合性を検証する。
- 将来の形式変更に備えて`schemaVersion`を必須とする。

## 9. 保存方式とサーバー構成

### 9.1 配信

- HTML、CSS、JavaScript、初期JSON、初期画像を静的ファイルとして配信する。
- 独自のアプリケーションサーバーは用意しない。
- ソースコードと仕様書はGitHubリポジトリで管理する。
- 本番サイトはGitHub Pagesへ配置する。
- `main`ブランチへの反映を起点に、GitHub Actionsでビルドとデプロイを行う。
- GitHub Pagesのプロジェクトサイトとして公開する場合のサブパスを考慮して、アセットURL、PWAマニフェスト、Service Workerのスコープを設定する。
- リポジトリや公開ファイルへ、パスワード、アクセストークン、秘密鍵などの機密情報を含めない。

### 9.2 端末内保存

- マスターデータと画像の保存にはIndexedDBを使用する。
- 通常の計算入力と結果は永続化しない。
- ブラウザのサイトデータを削除すると編集データも消えるため、バックアップ機能を提供する。
- 別端末への移行はJSONの書き出しと読み込みで行う。

### 9.3 オフライン

- Service Workerでアプリ本体と初期データをキャッシュする。
- 一度正常に読み込んだ端末では、通信がなくても計算、データ編集、バックアップ書き出しを利用できるようにする。
- アプリ更新時は、利用可能な新バージョンがあることを通知して再読み込みを促す。
- ビルド内容からキャッシュ識別子を生成し、アプリ更新時に古いファイルが残り続けないようにする。

## 10. UI・操作要件

- 基準画面幅は360px以上とする。
- 横スクロールを通常操作に要求しない。
- タップ対象は十分な大きさと間隔を確保する。
- 通常の計算操作は、必要数の数値入力以外をタップだけで完結させる。
- データ編集では装備分類の入力のため文字入力を許容する。
- 色だけに依存せず、文字またはアイコンを併用して状態を伝える。
- 計算、保存、削除、復元の処理結果を画面上で明示する。
- エラー発生時に入力済みの計算条件を失わない。

## 11. エラー処理

| 状況 | 動作 |
|---|---|
| 必要数が不正 | 対象入力欄の近くに理由を表示し、有効になるまで自動計算を行わない。 |
| 対象設計図のステージがない | 設計図名を含む計算不能メッセージを表示する。 |
| 計算に時間がかかる | 計算中表示を継続する。入力が変わった場合は直前の計算を破棄する。 |
| 計算内部エラー | 最適解を表示せず、再試行を案内する。 |
| アイコン読み込み失敗 | 代替アイコンを表示し、他のデータは利用可能にする。 |
| IndexedDB保存失敗 | 保存できなかったことを表示し、成功扱いにしない。 |
| 不正なバックアップ | 現在のデータを変更せず、検証エラーを表示する。 |
| 未対応の新しいバックアップ形式 | 対応していない形式であることを表示する。 |
| 初期データ不整合 | 計算を停止し、初期データの再読み込みを案内する。 |

## 12. 受け入れ条件

### 12.1 計算

- 1種類の設計図と必要数を入力し、必要数を満たす最小周回結果が得られる。
- 複数種類の設計図を入力し、同時ドロップを考慮した最小周回結果が得られる。
- 1周で対象2種類が得られる場合、別々のステージを1周ずつ回る結果より、その1ステージを1周する結果を選ぶ。
- 必要数を超える場合、獲得数と余剰数が正しく表示される。
- 総周回数が同じ複数解から、定義した決定規則で毎回同じ1件が選ばれる。
- ドロップ率は100%として計算される。
- 計算対象外の設計図を副産物として得ても、第一目的の総周回数に影響しない。
- 入手手段のない設計図を指定すると計算不能になる。
- 設計図の追加、削除または必要数の変更後に、ボタン操作なしで結果が更新される。

### 12.2 データ編集

- 新しい設計図をランク、装備分類、アイコンから追加でき、名称が自動生成される。
- 同じランクと装備分類の設計図を重複登録できない。
- 登録画像が変形や切り抜きなしで縮小表示される。
- 設計図のランク、装備分類、画像を編集できる。
- 未参照の設計図を削除できる。
- 参照中の設計図の削除が防止される。
- 新しいステージと1件以上のドロップを追加できる。
- 同じ章番号とステージ番号のステージを重複登録できない。
- 既存ステージの章番号、ステージ番号、ドロップを編集できる。
- ステージを削除すると以後の計算対象から除外される。
- 編集内容がブラウザを閉じて再度開いた後も維持される。

### 12.3 バックアップ

- 全マスターデータと画像を1つのJSONへ書き出せる。
- 書き出したJSONを読み込み、同じデータを復元できる。
- 不正なJSONを読み込んでも現在のデータが失われない。
- 初期データへのリセット後にWiki由来の初期データへ戻る。

### 12.4 スマートフォンとオフライン

- 360px幅で主要画面に意図しない横スクロールが発生しない。
- 通常利用では、必要数の数値入力以外をタップ操作で完結できる。
- 一度読み込んだ後、オフライン状態でも計算とデータ閲覧ができる。

## 13. 初期データ作成方針

- 参照Wikiの「素材ドロップ場所一覧」を正規化して収録する。
- 各行の「場所」をステージ名として登録する。
- 各行の「設計図」と「副産物」を、それぞれ数量1のドロップとして登録する。
- Wiki上の設計図コード、ランク、装備分類、名称の対応を保持する。
- 初期データ作成後、元表の全ステージ件数とアプリデータ件数を照合する。
- 各ステージについて、設計図と副産物の両方が正しく対応していることを検証する。
- 初期アイコンを同梱する場合は、ファイル名と設計図IDの対応を検証する。
- 初期設計図には参照Wiki掲載のWebPアイコンを設計図コード単位で同梱し、オフラインキャッシュへ含める。
- 利用者が登録した画像がある場合は登録画像を優先し、削除すると同梱アイコンへ戻す。
- Wikiの更新は自動反映せず、データ編集または新しい初期データのリリースで対応する。

## 14. 実装マイルストーン

### M1: データ基盤

- 初期ドロップテーブルの作成
- 設計図、ステージの型定義
- 初期データの整合性検証

### M2: 最小周回計算

- 整数最適化の実装
- 同率解の決定規則
- 自動テスト

### M3: 通常利用画面

- 設計図選択
- 必要数入力
- 計算結果表示
- スマートフォンレイアウト

### M4: データ編集

- 設計図の追加、編集、削除
- アイコン登録
- ステージとドロップの追加、編集、削除
- IndexedDB保存

### M5: バックアップとPWA

- JSON書き出し、読み込み
- 初期データへのリセット
- オフライン対応
- GitHub ActionsによるGitHub Pagesへの配置確認

## 15. 確定事項

- 最適化対象は総周回数のみとする。
- ドロップ率は100%とする。
- Wiki表の設計図と副産物を1周につき各1個獲得する。
- 同率の最適解は代表1件だけを表示する。
- 余剰数を表示する。
- 未解放ステージの除外機能は設けない。
- 必要数は数値入力のみとし、増減ボタンは設けない。
- 必要数の変更後は操作ボタンなしで自動計算する。
- 設計図名とステージ名は入力させず、各属性から自動生成する。
- 表示順は入力させず、確定した規則で自動整列する。
- 設計図とステージは追加、編集、削除可能とする。
- アイコン画像は切り抜かず、縦横比を維持して縮小表示する。
- バックアップと復元はJSONファイルで行う。
- 個人利用とし、通常の計算内容は保存しない。
- 専用サーバーを必要としない静的Webアプリとする。
